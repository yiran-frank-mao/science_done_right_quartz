## Equalizers

**Def**  <i><u>Equalizer</u></i>
In any category $\mathbf{C}$, given parallel morphisms $f,g \colon A\to B$, an equalizer of $f$ and $g$ consists of $E$ and $e \colon E \to A$, universal such that:$$f\circ e=g\circ e$$That is, given $z \colon Z \to A$ with $f \circ z = g \circ z$ there is a unique $u \colon Z \to E$ with $e \circ u = z$ all as in the diagram:
![eqlizer|300](https://svgshare.com/i/14BP.svg)
<u><b>e.g.</b></u> 
- In $\mathbf{Sets}$, given functions $f, g \colon A \to B$, their equalizer is the injection into $A$ of the equationally defined subset $e\colon E=\{x\in A\mid f(x)=g(x)\}\hookrightarrow A$. Since if $fh(z) = gh(z)$ for some $h \colon Z \to A$, then $h(z) ∈ E$ for all $z\in Z$. Then there is a unique $u \colon Z \to E$ with $z \mapsto b$ such that $e(b)=h(z)$ as $e$ is [[Morphisms#^4e0259|monic]].

**Prop**  In any category $\mathbf{C}$, if $e \colon E \to A$ is an equalizer of some pair of morphisms, then $e$ is [[Morphisms#^4e0259|monic]].
**Prop**  

**Prop**  Given a set $A$, every subset $U ⊆ A$ is an equalizer for some pair of functions.
**Proof**  Let set $2=\{\top,\bot\}$. Then consider the characteristic function $\chi_{U}\colon A\to2$:$$\chi_{U}(x)=\begin{cases}\top&x\in U\\\bot&x\notin U\end{cases}$$Then we have $U=\{x\in A\mid\chi_U(x)=\top\}$. So the following is an equalizer: $$$$Moreover, for every function $\varphi\colon A\to2$ we can form the equational subset: $$V_\varphi=\{x\in A\mid\varphi(x)=\top\}$$as an equalizer, in the same way.

**Prop**  Write $P(A)$ as power set of $A$, we have $\Hom(A, 2) \cong P (A)$ in $\mathbf{Sets}$.
**Proof**  Inherit the notation from the above proof, clearly we can derive that $\chi_{U}$ and $V_{\phi}$ are mutually inverse. Then $$f \colon \Hom(A,2) \to P(A), \quad \varphi \mapsto V_{\phi} $$indeed forms an isomorphism. Thus $\Hom(A, 2) \cong P (A)$.

**Prop**  In any category $\mathbf{C}$, if $e \colon E \to A$ is an equalizer of some pair of morphisms, then $e$ is [[Objects and Elements#^4e0259|monic]].
**Proof**  Suppose $e$ is the equalizer of $f,g\colon A\to B$. Then for all object $Z$ with morphisms $x,y \colon Z\to E$ and $e\circ x = e\circ y$, we have $$f \circ (e \circ x) = (f\circ e) \circ x = (g \circ e) \circ x = g \circ (e\circ x)$$Hence there exists unique $u \colon Z \to E$ with $e \circ u = e \circ x = e \circ y$, that is $x=y=u$. ^cbde93

**Prop**  In the category $\mathbf{Ab}$ of abelian groups, given $f,g \colon A \to B$, the arbitrary homomorphism $h\colon \ker(f-g) \to A$ forms an equalizer:
![ker|350](https://svgshare.com/i/14C2.svg)

## Coequalizers

**Def**  <i><u>Coequalizer</u></i>
For any parallel morphisms $f,g \colon A \to B$ in a category $\mathbf{C}$, a coequalizer consists of $Q$ and $q \colon B \to Q$, universal with the property $q\circ f = q \circ g$, as in ![|300](https://svgshare.com/i/14Cn.svg)That is, given any $Z$ and $z \colon B \to Z$, if $z\circ f = z\circ g$, then there exists a unique $u \colon Q \to Z$ such that $u\circ q = z$. ^9b92cf

**Prop**  If $q \colon B \to Q$ is a coequalizer of some pair of morphisms, then $q$ is epic.
**Proof**  Observe by duality, we know that such a coequalizer $q$ in a category $\mathbf{C}$ is an equalizer in $\mathbf{C}^\text{op}$, hence monic by the [[Mathematics/Category Theory/Duality#^cbde93|proposition]], and so $q$ is epic in $\mathbf{C}$.
<u><b>e.g.</b></u>  
- Let $R ⊆ X \times X$ be an equivalence relation on a set $X$ with $r_{1},r_{2}\colon R \to X$ are the two projections of the inclusion $i\colon R \to X \times X$. The canonical projection $\pi\colon X\to X/R$ defined by $x\mapsto [x]$ is then a coequalizer: ![coeq_set.svg|65%](https://raw.githubusercontent.com/yiran-frank-mao/image_repo/master/Obsidian/coeq_set.svg)

  - The coequalizer in $\mathbf{Sets}$ of an arbitrary parallel pair of functions $f,g \colon A\to B$ can be constructed by quotienting $B$ by the equivalence relation generated by the equations $f(x) = g(x)$ for all $x ∈ A$.

**Prop**  For every [[Free Groups and Relations#^587eee|monoid]] $M$ there are sets $G$ and $R$ and a coequalizer diagram, with $F (G)$ and $F (R)$ free, thus $M \cong F (G)/(r1 = r2)$.